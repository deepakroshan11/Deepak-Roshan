# Optional overrides for Umami (defaults are set in src/lib/umami.ts).
# NEXT_PUBLIC_UMAMI_WEBSITE_ID=
# NEXT_PUBLIC_UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
#
# Never put Umami API keys in NEXT_PUBLIC_* — they would leak to the browser.
